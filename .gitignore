node_modules/
npm-debug.log*
.env
.env.local
.vercel
data/alerts/
data/database.json
# Camera snapshots are private user data and must never be committed.
public/uploads/
VoiceForge/

# OS junk
.DS_Store

# Local agent tooling (not part of the application)
.claude/
.agents/
skills-lock.json
